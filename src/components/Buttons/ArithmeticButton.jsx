import React from 'react';
import Button from './Button';

const ArithmeticButton = (props) => {

    

    return (
        <Button buttonClick={} label={props.label}/>
    )
}

export default ArithmeticButton;